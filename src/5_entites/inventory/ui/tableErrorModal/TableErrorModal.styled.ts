import styled from '@emotion/styled';
import { Box, Button } from '6_shared';

export const ErrorModalCloseButton = styled(Button)`
  width: 30%;
  height: 20%;
  position: absolute;
  bottom: 25px;
  right: 25px;
`;
