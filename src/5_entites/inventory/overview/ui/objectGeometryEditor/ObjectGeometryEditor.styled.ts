import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const ObjectGeometryEditorStyled = styled(Box)`
  width: 100%;
  height: 100%;
  overflow: hidden;
`;

export const GeometryEditWrapper = styled(Box)`
  height: 100%;
  overflow-y: scroll;
`;
