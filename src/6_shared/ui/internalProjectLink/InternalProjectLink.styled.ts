import styled from '@emotion/styled';
import { Typography } from '@mui/material';

export const InternalProjectLinkStyled = styled(Typography)`
  cursor: pointer;
  margin: 0 3px;
  &:hover {
    text-decoration: underline;
  }
`;
