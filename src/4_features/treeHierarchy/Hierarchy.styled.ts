import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const ItemRightSideStyled = styled(Box)`
  display: flex;
  flex-direction: row;
  font-size: 12px;
  overflow: hidden;
  line-height: 30px;
  font-weight: 500;
  align-items: center;
  gap: 0.1rem;
  flex-shrink: 0;
  color: inherit;
`;
