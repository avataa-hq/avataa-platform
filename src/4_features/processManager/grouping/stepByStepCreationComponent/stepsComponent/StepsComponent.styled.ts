import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const StepComponentItemStyled = styled(Box)`
  padding: 10px;
  border-radius: 10px;
  cursor: pointer;
  flex: 1;
  text-align: center;

  box-shadow: 13px 14px 11px -10px rgba(0, 0, 0, 0.12);
`;
